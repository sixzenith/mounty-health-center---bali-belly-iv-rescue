
export interface Feature {
  icon: string;
  title: string;
  description: string;
}

export interface Testimonial {
  text: string;
  author: string;
  location: string;
  rating: number;
}

export interface TrustBadge {
  icon: string;
  text: string;
  subtext: string;
}
